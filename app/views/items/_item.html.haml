%div.content.item
  %p.image=image_tag item.assets.first.image.url(:medium)
  %p=link_to item.name, item_path(item)
  %p=number_to_currency(item.price)
  %p=item.description
  -if user_signed_in?
    %p=link_to "[Edit]",edit_item_path(item)
  %p.clear
