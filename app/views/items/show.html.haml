%div.content.item
  -@item.assets.each_slice(3) do |group|
    -group.each do |asset|
      =image_tag asset.image.url(:medium)
  %p=@item.name
  %p=item_price(@item)
  %p=@item.description
  %p
    Contact #{mail_to 'shandysdesign@gmail.com'} with questions and orders
